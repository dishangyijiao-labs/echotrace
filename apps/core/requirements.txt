fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
mcp>=1.20.0
requests>=2.32.0

# faster-whisper is installed separately due to build requirements.
# See install-python312.sh for the required installation order.
